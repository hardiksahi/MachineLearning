[tool.kedro]
package_name = "home_credit_risk_model_stability"
project_name = "home_credit_risk_model_stability"
kedro_init_version = "0.18.12"

[tool.isort]
profile = "black"

[tool.pytest.ini_options]
addopts = """
--cov-report term-missing \
--cov src/home_credit_risk_model_stability -ra"""

[tool.coverage.report]
fail_under = 0
show_missing = true
exclude_lines = ["pragma: no cover", "raise NotImplementedError"]
